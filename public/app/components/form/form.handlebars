<form id="{{id}}" class="{{customClasses}}" method="{{method}}" action="{{action}}">
    {{#each formControls}}
        <div class="form__group {{customClasses}}">
            {{{content}}}
        </div>
    {{/each}}
</form>
