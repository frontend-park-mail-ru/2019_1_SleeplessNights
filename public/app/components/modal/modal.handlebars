<div class="modal {{customClasses}}" id="{{id}}">
    <div class="modal__content">
        <div class="modal__close" id="close_{{id}}">
            <i class="material-icons">close</i>
        </div>
        {{#if header}}
            <div class="modal__header">
                {{header}}
            </div>
        {{/if}}
        <div class="modal__body">
            {{{body}}}
        </div>
    </div>
</div>
